// This service is no longer used in the client-side only version of the app.
// The AI generation features have been removed to allow static deployment on GitHub Pages without backend requirements.

export {};
